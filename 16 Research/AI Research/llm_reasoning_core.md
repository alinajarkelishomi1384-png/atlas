# لایه استدلال مرکزی (LLM Core)

---
title: "مفاهیم پایه‌ای و فلسفه ساخت هسته استدلال ماژولار"

# 📖 فاز صفر: درک مفاهیم کلان مغز متفکر پرنده

این مستند به زبان ساده، سنگ‌بنای پروژه را بدون وارد شدن به جزئیات کدنویسی تشریح می‌کند تا نقشه ذهنی طراح سیستم کامل باشد.

## ۱. ساختار هسته با LangGraph
ساخت هسته مستقل از پلتفرم، با نوشتن فرمول شروع نمی‌شود؛ بلکه با طراحی «جریان منطقی فکر خلبان» روی یک ماشین حالت (State Machine) به کمک ابزار LangGraph آغاز می‌گردد. این ابزار وظیفه شاسی‌کشی و تعیین مسیرهای مجاز تفکر را بر عهده دارد.

## ۲. شبکه عصبی و مدل زبانی در نقش خلبان
در این معماری، ما شبکه عصبی را از صفر نمی‌سازیم. بلکه از مدل‌های زبانی بزرگ (LLM) آماده به عنوان «موتور تفکر» استفاده می‌کنیم. برای حل مشکل عدم دانش هوش مصنوعی نسبت به پرنده‌های مختلف، از سیستم RAG (کارت حافظه دانش) استفاده می‌شود تا هوش مصنوعی کتابچه راهنمای هر پرنده را در لحظه خطا مطالعه و بر اساس آن استدلال کند.

## ۳. مقایسه متدهای ساخت مغز تصمیم‌گیری
1. **سیستم‌های سنتی (Rule-Based):** بر پایه کدهای سخت‌گیرانه `if/else`. غیرماژولار و ناتوان در برابر شرایط پیش‌بینی‌نشده.
2. **یادگیری تقویت‌شده (Reinforcement Learning):** هوشمند اما غیرقابل تایید از نظر ایمنی (جعبه سیاه).
3. **معماری پیشنهادی ما (Neuro-Symbolic):** ترکیب ساختار قطعی لانگ‌گراف با قدرت تفکر مدل زبانی. این تنها روشی است که اجازه می‌دهد هسته، ثابت و ماژولار بماند و هم‌زمان ایمنی آن تضمین شود.


---
title: "کالبدشکافی عمیق سلول‌های استدلال و موتور تصمیم‌گیری هوشمند"


# 🔍 تحلیل ساختاری و عمیق مکانیسم هسته استدلال مستقل

این مستند، لایه‌های پنهان پردازش داده، نحوه مهار مدل‌های زبانی بزرگ و مکانیسم ریاضی بازیابی دانش (RAG) را در هسته تصمیم‌گیری چندگانه پرواز تشریح می‌کند.

## ۱. مکانیسم استدلال شبکه‌های عصبی (Semantic Understanding)
موتور اصلی تفکر در این هسته، توانایی مدل‌های زبانی (LLMs) در درک مفاهیم انتزاعی و روابط معنایی (Contextual Relations) است. برخلاف سیستم‌های مبتنی بر کدنویسی کلاسیک که به کلیدواژه‌های سخت‌کد شده نیاز دارند، شبکه عصبی قادر است شرایط بحرانی ناآشنا، نویزهای حسگرها و تداخل‌های اطلاعاتی را به صورت مفهومی تحلیل کرده و سناریوی بحران را درک کند.

---

## ۲. قفس منطقی با تکامل گراف حالت (LangGraph Restrictions)
برای حذف ریسک توهم (Hallucination) و رفتارهای غیرقابل پیش‌بینی هوش مصنوعی در سیستم‌های حساس هوانوردی، از معماری LangGraph به عنوان یک قفس منطقی قطعی استفاده می‌شود:
- **محدودسازی خروجی‌ها (Output Constraints):** هوش مصنوعی در هر گره (Node) مجبور است خروجی خود را در قالب یک ساختار داده (Schema) کاملاً مشخص و محدود ارسال کند.
- **کنترل مسیر حرکت (Deterministic Edges):** هدایت پرنده و سوئیچ بین حالت‌های پروازی توسط فلش‌های قطعی گراف کنترل می‌شود، نه توسط میل هوش مصنوعی. هوش مصنوعی فقط مشاور داخل گره‌هاست، نه حاکم مسیر.

---

## ۳. مکانیسم برداری مموری دانش (Vector-Based RAG Mechanics)
ماژولار بودن دانش پرنده‌ها از طریق فرآیند جاسازی برداری (Vector Embedding) محقق می‌شود:
1. **تجزيه مستندات (Chunking):** کتابچه‌های راهنمای پرنده (POH/QRH) به تکه‌های اطلاعاتی کوچک شکسته می‌شوند.
2. **نگاشت ریاضی (Vectorization):** این تکه‌ها به بردار تبدیل شده و در یک پایگاه داده برداری (Vector DB) ذخیره می‌شوند.
3. **بازیابی لایو (Semantic Retrieval):** به محض وقوع ناهنجاری، وضعیت انتزاعی سیستم به یک بردار پرس‌وجو تبدیل شده و مرتبط‌ترین بخش کتابچه را در کمتر از چند میلی‌ثانیه فراخوانی کرده و به لایه استدلال تزریق می‌کند. این امر به هسته ثابت اجازه می‌دهد بدون تغییر در ساختار، با تعویض پایگاه داده، از پهپاد به هواپیما سوئیچ کند.

---

# 🌐 لایه‌های پیشرفته عملکردی هسته استدلال (Deep Dive Extension)

این بخش زیرسیستم‌های حیاتی اتصال هوش مصنوعی به فیزیک پرنده، مدیریت حافظه لایو و اعتبارسنجی منطقی را تشریح می‌کند.

## ۱. لایه انتزاع معنایی (Semantic Abstraction Layer)
هسته تصمیم‌گیری به صورت مستقیم با فرکانس‌های بالای داده‌های تلمتری (Telemetry) درگیر نمی‌شود. یک لایه واسط (Agnostic Wrapper) وظیفه دارد داده‌های عددی خام را به برچسب‌های معنایی (Semantic Tokens) تبدیل کند:
- **داده خام:** `Altitude: 45m, Vertical Speed: -12m/s, Terrain: Rocky`
- **ترجمه معنایی برای هسته:** `[FLIGHT_PHASE: Landing, DESCENT_RATE: Dangerously High, RISK: Hard Impact]`
این متدولوژی باعث می‌شود که هسته استدلال نیازی به دانستن ابعاد دقیق سنسورهای یک پرنده خاص نداشته باشد و فقط روی مفاهیم بحران تمرکز کند.

## ۲. مدیریت حافظه زنده مأموریت (State Memory Threading)
در معماری LangGraph، حالت سیستم (`State`) به صورت یک نخ حافظه (Thread) دائم در جریان است. این حافظه به دو بخش تقسیم می‌شود:
- **حافظه کوتاه‌مدت (Short-term Context):** پارامترهای پروازی ۱۰ ثانیه اخیر برای تشخیص ترندها (مثلاً آیا افت ولتاژ لحظه‌ای بوده یا به مرور زمان در حال رخ دادن است).
- **حافظه بلندمدت (Episodic Memory):** رویدادهای کلیدی کل مأموریت (مانند: رعد و برق در دقیقه ۱۲، خطای حسگر در دقیقه ۲۰). این داده‌ها به هوش مصنوعی کمک می‌کند تا زنجیره علت و معلولی خرابی‌ها را کشف کند.

## ۳. گره اعتبارسنجی منطقی خروجی (The Logic Gate Validation)
آخرین گره در گراف حالت، پیش از ارسال دستور به فیلتر فیزیکی (CBF)، گره داوری منطقی است. این گره بر اساس قوانین تغییرناپذیر هوانوردی (Determinism Hard-rules) عمل می‌کند.
- **وظیفه:** خروجی متنی مدل هوش مصنوعی را پارس (Parse) کرده و با «مجموعه اعمال ممنوعه» چک می‌کند. 
- *مثال:* اگر مدل زبانی تحت تاثیر نویز شدید، دستور `SHUTDOWN_ENGINES` را در فاز پرواز صادر کند، این لایه به صورت ساختاری فرمان را بلوکه کرده، گراف را به حالت `RE-EVALUATE` (ارزیابی مجدد) می‌فرستد و به خلبان گزارش خطا می‌دهد.



---

## 📊 خلاصه ساختار ۵ گانه هسته استدلال مستقل (System Component Blueprint)
در نهایت، هسته استدلال مستقل از پلتفرم پروژه ما، از ۵ بخش اصلی تشکیل می‌شود که به صورت یک زنجیره متوالی کار می‌کنند:
1. **Agnostic Wrapper (مترجم حسگرها):** تبدیل تلمتری عددی به توکن‌های معنایی متنی.
2. **State Memory Threading (حافظه زنده):** نگهداری تاریخچه کوتاه‌مدت و بلندمدت مأموریت.
3. **LangGraph Nodes (گره‌های تفکر):** هدایت ساختاریافته و بدون توهم مدل زبانی (LLM).
4. **Knowledge Retrieval / RAG (کارت حافظه):** بازیابی آنی مشخصات فنی و دستورالعمل‌های اختصاصی پلتفرم متصل‌شده.
5. **Logic Gate Validator (گره داوری):** فیلتر قطعی فرمت خروجی بر اساس قوانین هاردکد شده هوانوردی.


---

## ⚖️ مرزبندی تصمیم‌گیری: سهم هسته ثابت در برابر مموری کارت (Decoupled Reasoning)
برای درک دقیق عملکرد، مبنای تصمیم‌گیری سیستم به دو سطح کاملاً مجزا تقسیم می‌شود:

1. **منطق استدلال کلان (Inherent Core Logic):** قوانین هوانوردی جهانی که در خودِ هسته (LangGraph) تعبیه شده‌اند و به نوع پرنده وابسته نیستند (مانند ضرورت لغو مأموریت در صورت بروز خطای بحرانی درجه یک یا اولویت‌بندی حفظ پایداری بر ادامه تصویربرداری).
2. **پایگاه دانش پلتفرم (Injectable Knowledge Base):** پارامترهای اجرایی، کدهای خطا و متدهای فیزیکی که صرفاً از طریق **RAG (مموری کارت)** به هسته تزریق می‌شوند. هسته استدلال می‌داند که "باید فرود اضطراری کند" (دانش هسته)، اما اینکه "با چه زاویه‌ای و در چه باندی فرود بیاید" را از مموری کارت همان پرنده استخراج می‌کند. این تفکیک، ضامن اصلی مستقل از پلتفرم بودن سیستم است.



### 🔄 کالبدشکافی عمیق لایه اول: Agnostic Wrapper (مترجم پویا)

لایه اول به عنوان واسط سخت‌افزار و نرم‌افزار هوشمند، وظیفه تبدیل تلمتری عددی فرکانس‌بالا به کدهای معنایی فرکانس‌پایین را دارد. این لایه از ۳ زیربخش اصلی تشکیل شده است:

۱. **لایه پایش و فیلترینگ (Throttling Node):** کاهش فرکانس داده‌های سنسورها از فرکانس‌های کنترل فیزیکی (مثلاً ۴۰۰ هرتز) به فرکانس‌های استدلال شناختی (۱ تا ۲ هرتز) جهت جلوگیری از سرریز داده در مدل زبانی.
۲. **تطبیق‌دهنده پویا (Dynamic Threshold Engine):** این بخش مقادیر عددی (مانند ولتاژ، جریان، شتاب جی) را دریافت کرده و با خط‌کش‌های فیزیکی پلتفرم مقایسه می‌کند. نقطه قوت اینجاست که این خط‌کش‌ها (Thresholds) به صورت هاردکد نیستند، بلکه به محض اتصال پلتفرم، از کارت حافظه دانش (RAG) بارگذاری می‌شوند.
۳. **تولیدکننده وضعیت انتزاعی (Abstract State Generator):** خروجی نهایی این لایه، یک ساختار داده (مانند JSON) حاوی توکن‌های معنایی استاندارد هوانوردی است که به عنوان ورودی (Input State) به هسته LangGraph تزریق می‌شود.


---

### 🎛️ جزئیات ریاضی و ساختار داده لایه اول (Advanced Agnostic Engineering)

این بخش مستندات فنی مربوط به فرآیند بافرینگ زمان‌واقعی، پارسینگ داینامیک ماتریس نگاشت و ساختاردهی توکن‌ها برای ورود به گره‌های استدلال را تشریح می‌کند.

#### ۱. الگوریتم بافرینگ پنجره متحرک (Sliding Window & Signal Conditioning)
برای مهار نویزهای فرکانس‌بالا و نوسانات گذرای حسگرها، لایه اول مجهز به یک بافر متحرک زمانی ($W_{t}$) با طول ثابت (مثلاً ۵۰۰ میلی‌ثانیه) است. داده‌های ورودی از پروتکل‌های هوانوردی (MAVLink/CAN) ابتدا از فیلترهای پایداری عبور کرده و فرکانس استخراج حالت به $2\text{ Hz}$ کاهش می‌یابد تا پدیده نویز در استدلال رخ ندهد.

#### ۲. ساختار داینامیک ماتریس نگاشت تلمتری (Telemetry-to-Semantic JSON Schema)
لایه اول به صورت خودکفا هیچ فرضی در مورد سطوح ولتاژ، دمای پیشرانه یا محدودیت‌های آیرودینامیکی ندارد. این لایه صرفاً یک مفسر است که فایل پیکربندی توکن‌ها (Mapping Matrix) را از لایه RAG پرنده فراخوانی کرده و داده‌های عددی را بر اساس زون‌های تعیین‌شده (`nominal`, `warning`, `critical`) به کدهای معنایی استانداردی چون `BATT_VOLTAGE_LOW` نگاشت می‌کند.

#### ۳. پروتکل پکینگ و تولید ورودی لانگ‌گراف (Serialization Protocol)
خروجی معنایی تولید شده برای کاهش سربار توکن‌ها (Token Overhead) در مدل زبانی، به فرمت متن آزاد (Free Text) فرستاده نمی‌شود؛ بلکه از طریق ساختار داده فشرده پایتون (`TypedDict`) سریال‌سازی شده و مستقیماً گره ریشه (Root Node) گراف حالت لانگ‌گراف را تغذیه می‌کند. این معماری تضمین می‌کند زمان پردازش لایه اول همواره کمتر از $5\text{ ms}$ باقی بماند.


